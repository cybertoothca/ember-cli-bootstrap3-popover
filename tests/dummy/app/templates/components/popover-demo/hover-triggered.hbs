{{! template-lint-disable }}
<div class="page-header">
  <h3>Hover Triggered Popover</h3>
</div>

<div class="row">
  <div class="col-sm-4 form-group">
    {{#twbs-popover content="Mouse out of text to hide" popoverTrigger="hover" as |popover|}}
      {{#popover.trigger}}
        Hover over this text for the popover.
      {{/popover.trigger}}
    {{/twbs-popover}}
  </div>
  <div class="col-sm-8">
    {{! @formatter:off }}
    <pre>\{{#twbs-popover content="Mouse out of text to hide" popoverTrigger="hover" as |popover|}}
      \{{#popover.trigger}} Hover over this text for the popover.
      \{{/popover.trigger}}
      \{{/twbs-popover}}
    </pre>
    {{! @formatter:on }}
  </div>
</div>