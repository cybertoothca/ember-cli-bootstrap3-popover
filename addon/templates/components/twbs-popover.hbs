{{yield
  (hash
    trigger=(component "twbs-popover/trigger" register=(action "setTriggerElement"))
    content=(component "twbs-popover/content" register=(action "setPopoverContent"))
    title=(component "twbs-popover/title" register=(action "setPopoverTitle"))
  )
  (action "hide")
  (action "show")
  (action "toggle")
}}
